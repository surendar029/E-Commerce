info:
  name: " Authenticate"
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8081/api/v1/auth/authenticate
  body:
    type: json
    data: |-
      {
        "username": "haris",
        "password": "password"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
