info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8081/api/v1/auth/register
  body:
    type: json
    data: |-
      {
        "username": "haris",
        "email": "haris",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
